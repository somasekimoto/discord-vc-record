node_modules/

# パッケージマネージャは pnpm に統一（サプライチェーン対策の設定が pnpm-workspace.yaml にあり、
# npm install だとその防御が丸ごと効かないため）。誤って生成した lockfile をコミットさせない。
package-lock.json
yarn.lock

.env
.env.local
recorder/recordings/
*.pcm
*.wav
.wrangler/
web/worker-configuration.d.ts
dist/
.DS_Store
.dev.vars

# 実値入り設定(テンプレは .example をコミット)
web/wrangler.toml
recorder/fly.toml
